<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>jama</groupId> <!-- Package name really has captial J -->
  <artifactId>jama</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

    <distributionManagement>
      <repository>
        <id>eu.fox7</id>
        <name>Fox Repository</name>
        <url>file:///home/mt/fox-repository</url>
      </repository>
    <snapshotRepository>
      <id>eu.fox</id>
      <name>Fox Repository</name>
      <url>file:///home/mt/fox-repository</url>
    </snapshotRepository>
  </distributionManagement>

  <name>JAMA</name>
  <url>http://math.nist.gov/javanumerics/jama/</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>3.8.1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  
  <build>
  	<resources>
      <resource>
        <directory> src/my-resources </directory>
        <excludes>
          <exclude>**/*.jar</exclude>
        </excludes>
      </resource>
    </resources>
  	
    <plugins>
      <!-- Unpack src/main/resources/*.jar to target/classes/ -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <phase>compile</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <tasks>
                <unzip dest="target/classes" overwrite="true">
                  <fileset dir="src/main/resources">
                    <include name="**.jar"/>
                  </fileset>
                </unzip>
              </tasks>
            </configuration>
          </execution>
        </executions>
      </plugin>
      
      <!-- assemble classes to jar -->
      <!--
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
           <execution>
              <phase>package</phase>
           </execution>
        </executions>
        <configuration>
          <descriptor>src/assemble/main.xml</descriptor>
        </configuration>
      </plugin>
      -->
    </plugins>
  </build>

</project>
