package eu.fox7.bonxai.converter.relaxng2xsd;

/**
 * XML XSDSchema: built in simpleType facet names
 * @author Lars Schmidt
 */
enum XMLSchemaBuiltInSimpleTypeFacetNames {

    fractionDigits,
    maxExclusive,
    maxInclusive,
    maxLength,
    minExclusive,
    minInclusive,
    length,
    minLength,
    pattern,
    totalDigits;
}
