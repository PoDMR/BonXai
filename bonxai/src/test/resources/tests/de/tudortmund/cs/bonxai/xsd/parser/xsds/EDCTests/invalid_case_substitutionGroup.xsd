<schema targetNamespace="http://www.w3.org/2001/XMLSchema" xmlns="http://www.w3.org/2001/XMLSchema" xmlns:ipo="http://www.example.org">

  <element name="purchaseOrder" type="Items"/>

  <element name="comment" type="string"/>

  <element name="shipComment" type="string" substitutionGroup="comment"/>

  <element name="customerComment" type="SKU" substitutionGroup="comment"/>

  <complexType name="Items">
    <sequence>
      <element name="item" minOccurs="0" maxOccurs="unbounded">
        <complexType>
          <sequence>
            <element name="productName" type="string"/>
            <element name="quantity">
              <simpleType>
                <restriction base="positiveInteger">
                  <maxExclusive value="100"/>
                </restriction>
              </simpleType>
            </element>
            <element name="USPrice"    type="decimal"/>
            <element name="comment" type="date" minOccurs="0"/>
            <element name="shipDate"   type="date" minOccurs="0"/>
            <element ref="comment"/>
          </sequence>
          <attribute name="partNum" type="SKU" use="required"/>
        </complexType>
      </element>
    </sequence>
  </complexType>

  <simpleType name="SKU">
    <restriction base="string">
      <pattern value="\d{3}-[A-Z]{2}"/>
    </restriction>
  </simpleType>

</schema>